#include <algorithm>
#include <iostream>

using namespace std;

int main()
{
    int a, b;

    cin >> a >> b;

    for (int i = 0; i < a; i++)
    {
        int c, d;
        cin >> c >> d;
    }
    
    cout << "비와이" << endl;

    return 0;
}